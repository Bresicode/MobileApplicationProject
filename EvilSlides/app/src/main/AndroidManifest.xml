<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.evilslides">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".game.view.SlideEditActivity"
            android:parentActivityName=".game.view.LibraryActivity"></activity>
        <activity android:name=".game.view.GameActivity"
            android:parentActivityName=".game.view.MenuActivity"/>
        <activity android:name=".game.view.LibraryActivity"
            android:parentActivityName=".game.view.MenuActivity"/>
        <activity android:name=".game.view.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>